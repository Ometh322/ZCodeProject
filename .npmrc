# Ensure npm installs the correct platform-specific optional dependency
# (e.g. @esbuild/linux-x64 in Docker, @esbuild/win32-x64 on Windows).
# Without this, a lockfile generated on Windows may pin win32-only binaries
# and fail with EBADPLATFORM inside a Linux container.
optional=true
